Earth-Link/
├── app/                     # Contains the main application files for the Next.js app
│   ├── layout.tsx          # Main layout component for the application
│   └── globals.css         # Global CSS styles
├── components/              # Reusable React components
│   ├── Map.tsx             # Component for rendering the map
│   └── MapControls.tsx     # Component for map controls (projection, atmosphere, etc.)
├── config/                  # Configuration files
│   └── mapStyles.ts        # Map styling configuration
├── lib/                     # Library files for database and other utilities
│   └── db.ts               # Database connection and functions
├── pages/                   # Next.js pages (not shown in the provided context)
├── public/                  # Static files (not shown in the provided context)
├── styles/                  # CSS styles (not shown in the provided context)
├── .gitignore               # Specifies files and directories to ignore in Git
├── package.json             # Project metadata and dependencies
├── postcss.config.js        # Configuration for PostCSS
├── README.md                # Project documentation
└── tsconfig.json            # TypeScript configuration
